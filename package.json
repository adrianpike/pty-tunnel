{
  "name": "pty-tunnel",
  "version": "0.0.2",
  "bin": {
    "pty-tunnel": "./index.js"
  },
  "description": "Provide a simple tunnel to a command run in a pty.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Adrian Pike <adrian@adrianpike.com>",
  "license": "ISC",
  "dependencies": {
    "node-pty": "^0.6.10"
  }
}
